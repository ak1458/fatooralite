# dependencies
**/node_modules/

# next
**/.next/
**/out/
**/next-env.d.ts

# env
**/.env
**/.env.*
!**/.env.example

# secrets — LOCAL ONLY, never commit, never ship to live
creds.md
**/creds.md
/archive/
**/*.secret

# test
**/coverage/
**/playwright-report/
**/test-results/


# os / editor
.DS_Store
Thumbs.db
*.log

# playwright MCP artifacts
.playwright-mcp/

# prisma local db
*.db
*.db-journal

# build caches
**/tsconfig.tsbuildinfo

# agent worktrees — full repo copies created by parallel-agent runs.
# Never commit these; `git worktree prune` + `git branch -D worktree-*` cleans up.
.claude/worktrees/
.serena/
**/.mcp.json
mcp_config.json
